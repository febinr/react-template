// import apiRequest from '../services/apiServices';


const initialState = {
 isLoading : false
};

const App = {
  state: { ...initialState },
  reducers: {
  
  },
  effects: dispatch => ({
    
  })
};

export default App;
